{
    "name": "DoughLoops2",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix client\" \"npm run start --prefix server\"",
        "lint": "npm run lint --prefix client",
        "format": "prettier --write .",
        "typecheck": "npm run typecheck --prefix client",
        "check": "npm run typecheck && npm run lint && npm run build --prefix client"
    },
    "devDependencies": {
        "concurrently": "^8.0.0",
        "prettier": "^3.5.3"
    }
}
